import React from "react";
import styled from "styled-components";

const StyledRegister = styled.form``;

export default function Register(props) {
  return <StyledRegister>Register</StyledRegister>;
}
